# Egress allow-list, one hostname per line (IPv4, resolved when the container starts; no wildcards).
# Baked into the image on purpose: the agent can edit the workspace but not this file, so widening
# the list is a human decision (edit, then "Dev Containers: Rebuild Container").
# GitHub is handled separately in init-firewall.sh from https://api.github.com/meta.

# Claude Code: API, and the login/token-exchange hosts (docs: network-config, "Network access requirements")
api.anthropic.com
claude.ai
platform.claude.com

# NuGet restore, dotnet tool restore and NuGet audit (verified by a clean restore in the container)
api.nuget.org

# VS Code Server and extension install when the container is opened from VS Code. Delete these three
# lines for a stricter setup if you only use the CLI (docker exec / devcontainer exec).
update.code.visualstudio.com
marketplace.visualstudio.com
vscode.blob.core.windows.net

# ---- OPTIONAL, disabled by default (see docs/DevContainer.md, "Documentation MCP servers") ----
# Enable BOTH the hosts below AND the matching entries in .mcp.json + .claude/settings.json together.
# learn.microsoft.com
# mcp.context7.com
