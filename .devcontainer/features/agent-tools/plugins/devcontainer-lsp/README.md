# devcontainer-lsp

Placeholder plugin directory: the language servers are declared in the marketplace entry
(`../.claude-plugin/marketplace.json`), the same way the official `csharp-lsp` plugin does it.
The binaries come from the dev container image (`.devcontainer/Dockerfile`).
