{
  "id": "lockdown",
  "version": "1.0.0",
  "name": "Privilege lockdown",
  "description": "Removes sudo and the ssh client, strips setuid/setgid bits, makes the toolchain root-owned and prepares the named-volume mount points. Must be the last Feature installed (devcontainer.json overrideFeatureInstallOrder).",
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils",
    "ghcr.io/devcontainers/features/dotnet",
    "ghcr.io/devcontainers/features/node",
    "ghcr.io/devcontainers/features/github-cli"
  ]
}
